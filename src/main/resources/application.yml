# DEV CONFIGURATION
spring:
  application:
    name: ams
  datasource:
    url: ${DB_URL}
    driver-class-name: org.postgresql.Driver

  # Spring Mail Config (For Bean Creation)
  mail:
    host: smtp.gmail.com
    port: 587
    username: stanleypanag18@gmail.com
    password: ${GOOGLE_APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
